import { Search } from "lucide-react";

const SearchMobile = () => {
	return (
		<section id="search">
			<Search className="icon text-white" />
			<p className="font-semibold text-white">Search</p>
		</section>
	);
};

export default SearchMobile;
